# Prompt لإنشاء واجهة Book Assistant Lite

## السياق

أنت مطلوب منك تصميم وكتابة واجهة ويب كاملة لتطبيق Spring Boot اسمه **Book Assistant Lite**. التطبيق يستخدم Thymeleaf كمحرك قوالب ويعمل مع ملفي `index.html` + `styles.css`.

البرنامج هو مساعد ذكاء اصطناعي لتحليل الكتب — يرفع المستخدم كتاباً بصيغة PDF ثم يستخدم 9 أدوات تحليل مختلفة مدعومة بـ AI.

---

## المتطلبات التقنية الصارمة

### Thymeleaf — ما يجب الالتزام به حرفياً

**متغيرات الـ Model المتاحة (th:text / th:if):**
```
${hasBook}          — boolean — هل يوجد كتاب محمّل في الجلسة
${bookLanguage}     — String: "ar" أو "en" — اللغة المكتشفة تلقائياً
${error}            — String — رسالة خطأ عند الرفع
${success}          — String — رسالة نجاح عند الرفع
${summaryGeneral}   — String — نتيجة الملخص العام
${summaryPages}     — String — نتيجة ملخص الصفحات
${realityCheck}     — String — نتيجة فحص الواقعية
${conceptMap}       — String — نتيجة خريطة المفاهيم
${riskFlags}        — String — نتيجة تنبيهات المخاطر
${actionPlan}       — String — نتيجة الخطة التنفيذية
${semanticSearch}   — String — نتيجة البحث الدلالي
${qa}               — String — نتيجة سؤال وجواب
${comprehension}    — String — نتيجة اختبار الفهم
```

**Endpoints الـ Form Actions (POST):**
```
/upload             — multipart/form-data — حقل: file (PDF)
/summary/general    — حقل: outputLanguage
/summary/pages      — حقول: startPage, endPage, outputLanguage
/reality-check      — حقول: startPage, endPage, outputLanguage
/concept-map        — حقول: startPage, endPage, outputLanguage
/risk-flags         — حقل: outputLanguage
/action-plan        — حقول: startPage, endPage, outputLanguage
/semantic-search    — حقول: query, outputLanguage
/qa                 — حقول: question, outputLanguage
/comprehension      — حقول: startPage, endPage, outputLanguage
```

**GET الرئيسي:**
```
/                   — الصفحة الرئيسية
```

**HTML الجذر المطلوب:**
```html
<html xmlns:th="http://www.thymeleaf.org"
      th:lang="${bookLanguage != null ? bookLanguage : 'ar'}"
      th:dir="${bookLanguage != null && bookLanguage == 'en' ? 'ltr' : 'rtl'}"
      lang="ar" dir="rtl">
```

**قواعد Thymeleaf:**
- `th:action="@{/endpoint}"` — على كل form
- `th:if="${hasBook}"` — لإخفاء/إظهار التبويبات
- `th:text="${variable}"` — لعرض النتائج
- كل form بـ `method="post"`
- form الرفع: `enctype="multipart/form-data"`
- الـ outputLanguage يُرسَل كـ hidden input مع كل form

---

## تصميم الواجهة المطلوب

### الهوية البصرية
- **الطابع:** أداة احترافية للمعرفة — تجمع بين عمق المكتبة وحداثة الـ AI
- **الثيم:** داكن أساسي مع لمسات من الذهبي/الكهرمان كـ accent — يعطي إحساس بالثراء المعرفي
- **الخط:** استخدم Google Fonts — اختر خطاً مناسباً للواجهة العربية/الإنجليزية
- **التأثيرات:** ناعمة ومقصودة — لا مبالغة

### بنية الصفحة المطلوبة

#### 1. الـ Header
- شعار نصي: "Book Assistant Lite" مع تصميم يعكس الهوية
- مؤشر حالة الكتاب المحمّل (يظهر اسم الملف أو "لا يوجد كتاب")

#### 2. منطقة رفع الكتاب
- منطقة drag-and-drop بصرياً جذابة
- مؤشر واضح للحالة: قبل الرفع / بعده (استخدم `${hasBook}` و `${success}` و `${error}`)
- يجب أن يظهر فيها نوع الكتاب المكتشف (عربي/إنجليزي) بعد الرفع

#### 3. شريط إعدادات اللغة
- يظهر فقط بعد رفع الكتاب (`th:if="${hasBook}"`)
- اختيار لغة الإجابة: العربية / English
- badge يعرض اللغة المكتشفة للكتاب

#### 4. التبويبات التسعة
الأدوات مجمّعة في **نظام tabs** (وليس grid كلها مرئية معاً) لأسباب تتعلق بالوضوح والتنظيم. اجعل التنقل بين التبويبات يعمل بـ JavaScript خالص (بدون إعادة تحميل الصفحة للتنقل — فقط عرض/إخفاء).

**مجموعة الملخص:**
- 📖 ملخص عام (زر واحد فقط)
- 📄 ملخص صفحات مختارة (startPage + endPage)

**مجموعة التحليل:**
- ✅ فحص الواقعية (startPage + endPage)
- 🗺️ خريطة المفاهيم (startPage + endPage)
- ⚠️ تنبيهات المخاطر (زر واحد)

**مجموعة التفاعل:**
- 📋 الخطة التنفيذية (startPage + endPage)
- 🔍 بحث دلالي (text input)
- ❓ سؤال وجواب (text input)
- 📝 اختبار الفهم (startPage + endPage)

#### 5. منطقة عرض النتائج
- عرض النتيجة بجانب أو أسفل الأداة النشطة
- النتيجة في `<pre>` منسق بشكل جيد
- مؤشر loading واضح أثناء الانتظار (بعد submit)
- رسالة واضحة لو لم تُنفَّذ الأداة بعد

### تفاصيل UX مهمة
1. **Loading state:** عند submit أي form، أظهر spinner أو نص "جاري التحليل..." وعطّل الزرار
2. **التمييز البصري بين الأدوات:** كل أداة لها لون أو أيقونة مميزة
3. **الـ Tab active state:** الـ tab الذي يحتوي على نتيجة يجب أن يظهر بشكل مختلف (dot أو لون مختلف)
4. **Responsive:** يعمل على الموبايل
5. **RTL/LTR:** الكود يتعامل مع الاتجاهين حسب `bookLanguage`
6. **عند وجود نتيجة من الـ server:** التطبيق يعمل بـ full page reload (Thymeleaf) — لذا عند العودة يجب أن تُفتح الـ tab الصحيح تلقائياً إذا كانت النتيجة موجودة في الـ model

### Auto-tab JavaScript Logic
```javascript
// بعد تحميل الصفحة، افتح الـ tab الذي يحتوي على نتيجة
// فحص: إذا كان العنصر th:text موجوداً وليس فارغاً → افتح الـ tab المقابل
// الأولوية: أحدث نتيجة
```

---

## ما يجب تسليمه

### الملف الأول: `index.html`
- Thymeleaf template كامل
- ضع الـ `<style>` الخاص بالتصميم داخل `<head>` مباشرة (بجانب أو بدلاً من `<link>`)
- أو استخدم `<link rel="stylesheet" th:href="@{/styles.css}">` مع ملف منفصل

### الملف الثاني: `styles.css`
- CSS كامل مع كل التفاصيل البصرية
- استخدم Google Fonts بـ `@import`
- CSS Variables للألوان والـ spacing
- Animations للـ loading states والـ transitions

---

## قيود إضافية

- **لا JavaScript frameworks** — Vanilla JS فقط
- **لا dependencies خارجية** عدا Google Fonts
- **لا Bootstrap / Tailwind** — CSS خالص
- الكود يجب أن يعمل مباشرة في Spring Boot Thymeleaf بدون تعديل
- **حافظ على كل الـ form fields والـ endpoints كما هي بالضبط** — أي تغيير في أسماء الـ inputs سيكسر الـ backend

---

## مثال على نتيجة جيدة

الواجهة الحالية تبدو كـ Bootstrap form بسيط. الهدف هو واجهة تبدو كأداة SaaS احترافية — نظيفة، مقصودة، تعكس أن البرنامج يتعامل مع المعرفة والكتب. شيء يجعل المستخدم يشعر أنه يستخدم أداة جادة.
